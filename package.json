{
    "scripts": {
      "up": "cd infrastructure && docker compose -f docker-compose.dev.yml up -d",
      "down": "cd infrastructure && docker compose -f docker-compose.dev.yml down",
      "logs": "cd infrastructure && docker compose -f docker-compose.dev.yml logs -f",
      "restart": "npm run down && npm run up",
      "deploy": "cd infrastructure && docker compose up --build -d"
    }
  }


